module.exports = {
  Downloader: require('./downloader'), // Exporting the Downloader class from the downloader module
  Launcher: require('./launcher') // Exporting the Launcher class from the launcher module
};
